{
  "collectionName": "components_sections_before_afters",
  "info": {
    "displayName": "Before / After Slider",
    "icon": "slideshow",
    "description": "Interactive before and after transformation slider"
  },
  "options": {},
  "attributes": {
    "heading": { "type": "string", "default": "Spatial Metamorphosis" },
    "description": { "type": "text" },
    "beforeImage": {
      "type": "media",
      "multiple": false,
      "required": false,
      "allowedTypes": ["images"]
    },
    "afterImage": {
      "type": "media",
      "multiple": false,
      "required": false,
      "allowedTypes": ["images"]
    },
    "beforeLabel": { "type": "string", "default": "Before" },
    "afterLabel": { "type": "string", "default": "After" }
  }
}
